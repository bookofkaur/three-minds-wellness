# Exclude compiled/minified build artifacts from secret scanning.
# The real secret lives in .env (gitignored); the build bundle is
# a compiled artifact — scanning it causes the token to be auto-revoked.
paths-ignore:
  - "dist/**"
  - "assets/**"
